# Minimal dependencies for local Qdrant seeding without Docker.
# Torch is pinned to the CPU wheel so pip does not pull multi-GB CUDA packages.
--extra-index-url https://download.pytorch.org/whl/cpu

pydantic>=2.10.0
pydantic-settings>=2.6.0
qdrant-client>=1.7
torch==2.12.0+cpu
sentence-transformers>=3.0
