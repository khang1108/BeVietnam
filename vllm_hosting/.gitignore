# Secrets and runtime artifacts — never commit.
.env
# setup.sh kept local at the user's request (not tracked).
setup.sh
.venv/
logs/
*.log
# cloudflared tunnel credential files (written to ~/.cloudflared by setup.sh,
# but ignore any local copies too)
*.json
