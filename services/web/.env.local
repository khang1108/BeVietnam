# Next.js exposes NEXT_PUBLIC_ vars to the browser

# Backend API base (FastAPI on the Azure VM, via cloudflared tunnel). No trailing slash beyond /api/v1.
NEXT_PUBLIC_API_URL=https://api.iamphuckhang.dev/api/v1

# This MUST be the Goong "Maptiles" key (not the REST API key) for map display
NEXT_PUBLIC_GOONG_API_KEY=jOuIgvfmiXHWmbRCwsViqJ0U9syt3nt1PVSzlrw2
