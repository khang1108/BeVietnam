# ── AI Core Dependencies ──────────────────────────────────────────────────────
# Core API framework
fastapi>=0.100
uvicorn>=0.20
python-multipart>=0.0.8
pydantic>=2.10.0
pydantic-settings>=2.6.0

# LLM integration: self-hosted vLLM (OpenAI-compatible) via httpx — see below.

# Agent workflow orchestration
langgraph>=0.2

# Vector database
qdrant-client>=1.7

# Embedding via HuggingFace Inference API (no local model — no torch/GPU needed)
httpx>=0.27.0

# Book ingestion pipeline (scripts/ingest_books.py)
pypdf>=4.0.0
python-docx>=1.1.0
